# ==============================================================================
#  requirements.txt  —  Streamlit Cloud ONLY
#  This is intentionally lightweight — no ML libraries needed.
#  The heavy pipeline (pmdarima, prophet, xgboost) runs on GitHub Actions,
#  not on Streamlit Cloud. app.py only reads pre-computed CSVs.
# ==============================================================================

streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
